import { createSlice } from "@reduxjs/toolkit";

const initialState = {}

const auth = createSlice({
    name: 'auth',
    initialState,
    reducers:{
        _stateAcount :(state,action) =>{
            state
        }
    }
})